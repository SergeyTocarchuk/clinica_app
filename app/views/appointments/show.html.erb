<%= form_with( model: @appointment, url: recommendations_path(@appointment), method: :patch) do |form| %>
  <%= form.label :recommendation, "Recommendations:" %>
  <%= form.text_field :recommendation %>
  <%= form.submit "Submit" %>
<% end %>